<div class="card">
  <div class="card-body">
    <%= simple_form_for job, url: url, action: :update, local: true do |f| %>
      <h4 class="text-center my-3">About your company values</h4>
      <% if job.errors.any? %>
        <%= render "/utils/validation_alert", resource: job  %>
      <% end %>

      <div class="mt-5">
        <h5 class="mb-3">Benefits provided</h5>
        <div class="row">
        <% Benefit.all.each_slice(5) do |benefits| %>
            <div class="col col-6">
              <% benefits.each do |benefit| %>
                <div class="custom-control custom-checkbox mr-3">
                  <input type="checkbox" <%= "checked" if job.benefits.include?(benefit) %> class="custom-control-input" value="<%= benefit.id %>" name="job[benefit_ids][]" id="job_benefits_<%= benefit.id %>">
                  <label class="custom-control-label" for="job_benefits_<%= benefit.id %>"><%= benefit.value %></label>
                </div>
              <% end %>
            </div>
        <% end %>
        </div>
      </div>
      <hr />

      <div class="mt-5">
        <h5 class="mb-3">List some of your company values:</h5>
        <div class="row">
        <% Culture.all.each_slice(5) do |cultures| %>
            <div class="col col-6">
              <% cultures.each do |culture| %>
                <div class="custom-control custom-checkbox mr-3">
                  <input type="checkbox" <%= "checked" if job.cultures.include?(culture) %> class="custom-control-input" value="<%= culture.id %>" name="job[culture_ids][]" id="job_cultures_<%= culture.id %>">
                  <label class="custom-control-label" for="job_cultures_<%= culture.id %>"><%= culture.value %></label>
                </div>
              <% end %>
            </div>
        <% end %>
        </div>
      </div>
      <%= f.hidden_field :navigate_to, value: "skills" %>

      <div class="text-center">
        <%= f.submit "Continue", class: "btn btn-primary btn-lg mt-4 mb-2" %>
      </div>

    <% end %>
  </div>
</div>
